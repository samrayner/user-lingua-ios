// Space.swift

import Foundation

extension CGFloat {
    /// Size constants for padding, spacing and margins.
    package enum Space {
        // swiftlint:disable identifier_name
        /// 2px
        package static let xxs: CGFloat = 2

        /// 4px
        package static let xs: CGFloat = 4

        /// 8px
        package static let s: CGFloat = 8

        /// 16px
        package static let m: CGFloat = 16

        /// 24px
        package static let l: CGFloat = 24

        /// 32px
        package static let xl: CGFloat = 32

        /// 64px
        package static let xxl: CGFloat = 64
        // swiftlint:enable identifier_name
    }
}
